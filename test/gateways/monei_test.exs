defmodule Kuber.Hex.Gateways.MoneiTest do
  use ExUnit.Case, async: false

  alias Kuber.Hex.{
    CreditCard,
    Address,
    Response
  }
  alias Kuber.Hex.Gateways.Monei, as: Gateway

  test "empty monei test" do
    assert false
  end
end
